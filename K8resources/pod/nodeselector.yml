apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: swiggy1
  name: swiggy-deploy
spec:
  replicas: 3
  selector:
    matchLabels:
      app: swiggy
  template:
    metadata:
      labels:
        app: swiggy
    spec:
      nodeSelector:  # Moved to the pod spec
        app: node1  # Label key and value for node selection
      containers:
      - name: cont1
        image: rahamshaik/trainservice:latest
        ports:
          - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: abc
spec:
  type: LoadBalancer
  selector:
    app: swiggy1
  ports:
    - port: 80
      targetPort: 80
